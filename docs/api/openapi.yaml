openapi: 3.1.0
info:
  title: API Sistema de Triage y Gestión de Solicitudes Académicas
  description: |
    API RESTful para la gestión estructurada de solicitudes académicas.
    **Dominio:** Gestión de Solicitudes Académicas
    **Agregado Principal:** Solicitud
  version: 1.0.0
  contact:
    name: Programa de Ingeniería de Sistemas
    email: sistemas@uniquindio.edu.co

servers:
  - url: http://localhost:8080
    description: Servidor de desarrollo local
  - url: https://api-triage.uniquindio.edu.co
    description: Servidor de producción

tags:
  - name: Solicitudes
    description: Gestión del agregado Solicitud
  - name: Tipos de Solicitud
    description: Catálogo de tipos de solicitud
  - name: Usuarios
    description: Gestión de usuarios del sistema

paths:
  /api/solicitudes:
    post:
      tags:
        - Solicitudes
      summary: Crear una nueva solicitud
      description: |
        Registra una nueva solicitud en estado REGISTRADA.
        Mapea a la operación del dominio: `Solicitud.crear()`
      operationId: crearSolicitud
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CrearSolicitudRequest'
            example:
              tipoSolicitud: HOMOLOGACION
              descripcion: "Solicito homologación de la asignatura Programación I cursada..."
              canalOrigen: CORREO
      responses:
        '201':
          description: Solicitud creada exitosamente
          headers:
            Location:
              description: URI de la solicitud creada
              schema:
                type: string
                example: /api/solicitudes/507f1f77bcf86cd799439011
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SolicitudDetalleResponse'
        '400':
          description: Datos de entrada inválidos
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado

    get:
      tags:
        - Solicitudes
      summary: Listar solicitudes (con paginación y filtros)
      operationId: listarSolicitudes
      parameters:
        - name: estado
          in: query
          schema:
            $ref: '#/components/schemas/EstadoSolicitud'
        - name: prioridad
          in: query
          schema:
            $ref: '#/components/schemas/Prioridad'
        - name: solicitanteId
          in: query
          schema:
            type: string
        - name: page
          in: query
          schema:
            type: integer
            default: 0
        - name: size
          in: query
          schema:
            type: integer
            default: 20
      responses:
        '200':
          description: Lista de solicitudes
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginaSolicitudes'

  /api/solicitudes/{id}:
    get:
      tags:
        - Solicitudes
      summary: Obtener detalle de una solicitud
      operationId: obtenerSolicitudPorId
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Detalle de la solicitud
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SolicitudDetalleResponse'
        '404':
          description: Solicitud no encontrada

  /api/solicitudes/{id}/clasificar:
    put:
      tags:
        - Solicitudes
      summary: Clasificar una solicitud
      operationId: clasificarSolicitud
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ClasificarSolicitudRequest'
      responses:
        '200':
          description: Solicitud clasificada exitosamente
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SolicitudDetalleResponse'

  /api/solicitudes/{id}/asignar:
    put:
      tags:
        - Solicitudes
      summary: Asignar responsable
      operationId: asignarResponsable
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AsignarResponsableRequest'
      responses:
        '200':
          description: Responsable asignado exitosamente

  /api/solicitudes/{id}/estado:
    patch:
      tags:
        - Solicitudes
      summary: Cambiar estado
      operationId: cambiarEstado
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CambiarEstadoRequest'
      responses:
        '200':
          description: Estado cambiado exitosamente

  /api/solicitudes/{id}/historial:
    get:
      tags:
        - Solicitudes
      summary: Obtener historial
      operationId: obtenerHistorial
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Historial de la solicitud
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/EventoHistorialResponse'


  /api/usuarios:
    post:
      tags:
        - Usuarios
      summary: Crear un nuevo usuario
      operationId: crearUsuario
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CrearUsuarioRequest'
      responses:
        '201':
          description: Usuario creado exitosamente
        '400':
          description: Error en la validación de datos o regla de negocio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /api/tipos-solicitud:
    get:
      tags:
        - Tipos de Solicitud
      summary: Listar todos los tipos de solicitud
      operationId: listarTiposSolicitud
      responses:
        '200':
          description: Lista de tipos obtenida exitosamente
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/TipoSolicitudDTO'

  /api/tipos-solicitud/{id}:
    put:
      tags:
        - Tipos de Solicitud
      summary: Actualizar un tipo de solicitud
      operationId: actualizarTipoSolicitud
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ActualizarTipoSolicitudRequest'
      responses:
        '200':
          description: Actualizado correctamente

  /api/usuarios/{id}:
    get:
      tags:
        - Usuarios
      summary: Obtener detalle de un usuario por identificación
      operationId: obtenerUsuarioPorId
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Datos del usuario encontrados
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UsuarioResponse'
        '404':
          description: Usuario no encontrado

  /api/usuarios/{id}/desactivar:
    patch:
      tags:
        - Usuarios
      summary: Desactivar un usuario del sistema
      description: Cambia el estado del usuario a inactivo si no tiene solicitudes pendientes.
      operationId: desactivarUsuario
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DesactivarUsuarioRequest'
      responses:
        '200':
          description: Usuario desactivado exitosamente
        '422':
          description: Error de regla de negocio (ej. tiene solicitudes activas)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'


components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  schemas:
    # Request DTOs
    CrearSolicitudRequest:
      type: object
      required: [tipoSolicitudId, descripcion, canalOrigen]
      properties:
        tipoSolicitudId:
          type: integer
          format: int64
        descripcion:
          type: string
          minLength: 20
          maxLength: 1000
        canalOrigen:
          $ref: '#/components/schemas/CanalOrigen'

    ClasificarSolicitudRequest:
      type: object
      required: [tipoSolicitudId, prioridad, justificacion]
      properties:
        tipoSolicitudId:
          type: integer
          format: int64
        prioridad:
          $ref: '#/components/schemas/PrioridadDTO'
        justificacion:
          type: string
          minLength: 10
          maxLength: 500

    AsignarResponsableRequest:
      type: object
      required: [responsableId]
      properties:
        responsableId:
          type: string

    CambiarEstadoRequest:
      type: object
      required: [nuevoEstado, observacion]
      properties:
        nuevoEstado:
          $ref: '#/components/schemas/EstadoSolicitud'
        observacion:
          type: string
          maxLength: 500

    # Response DTOs
    SolicitudDetalleResponse:
      type: object
      properties:
        id:
          type: string
        codigo:
          type: string
        tipo:
          $ref: '#/components/schemas/TipoSolicitudDTO'
        descripcion:
          type: string
        canalOrigen:
          $ref: '#/components/schemas/CanalOrigen'
        estado:
          $ref: '#/components/schemas/EstadoSolicitud'
        prioridad:
          $ref: '#/components/schemas/PrioridadDTO'
        solicitante:
          $ref: '#/components/schemas/UsuarioResumenDTO'
        responsable:
          $ref: '#/components/schemas/UsuarioResumenDTO'
        fechaCreacion:
          type: string
          format: date-time

    EventoHistorialResponse:
      type: object
      properties:
        secuencia:
          type: integer
        fechaHora:
          type: string
          format: date-time
        accion:
          $ref: '#/components/schemas/TipoAccion'
        usuarioNombre:
          type: string
        observacion:
          type: string

    PaginaSolicitudes:
      type: object
      properties:
        contenido:
          type: array
          items:
            $ref: '#/components/schemas/SolicitudResumenResponse'
        paginaActual:
          type: integer
        totalPaginas:
          type: integer

    SolicitudResumenResponse:
      type: object
      properties:
        id:
          type: string
        codigo:
          type: string
        estado:
          $ref: '#/components/schemas/EstadoSolicitud'
        prioridad:
          $ref: '#/components/schemas/Prioridad'

    # Objetos de Valor
    PrioridadDTO:
      type: object
      required: [nivel, justificacion]
      properties:
        nivel:
          $ref: '#/components/schemas/Prioridad'
        justificacion:
          type: string

    TipoSolicitudDTO:
      type: object
      properties:
        id:
          type: integer
          format: int64
        nombre:
          type: string

    UsuarioResumenDTO:
      type: object
      properties:
        id:
          type: string
        nombreCompleto:
          type: string

    # Enums
    EstadoSolicitud:
      type: string
      enum: [REGISTRADA, CLASIFICADA, ASIGNADA, EN_ATENCION, ATENDIDA, CERRADA]

    Prioridad:
      type: string
      enum: [CRITICA, ALTA, MEDIA, BAJA]

    CanalOrigen:
      type: string
      enum: [CSU, CORREO_ELECTRONICO, SAC, PRESENCIAL, TELEFONICO, WEB]

    TipoAccion:
      type: string
      enum: [CREACION, CLASIFICACION, ASIGNACION, CAMBIO_ESTADO, PRIORIZACION, CIERRE]

    ErrorResponse:
      type: object
      properties:
        timestamp:
          type: string
          format: date-time
        status:
          type: integer
        mensaje:
          type: string

    # --- DTOs de Tipos de Solicitud ---
    CrearTipoSolicitudRequest:
      type: object
      required: [nombre, descripcion]
      properties:
        nombre:
          type: string
          minLength: 3
          maxLength: 50
          example: "Homologación"
        descripcion:
          type: string
          maxLength: 255
          example: "Solicitud para validar materias de otras instituciones."

    ActualizarTipoSolicitudRequest:
      type: object
      required: [id, nombre, descripcion]
      properties:
        id:
          type: integer
          format: int64
          examples: [1]
        nombre:
          type: string
          minLength: 3
          maxLength: 50
          example: "Homologación Externa"
        descripcion:
          type: string
          example: "Nueva descripción para el proceso de homologación."

    # --- DTOs de Usuario ---
    CrearUsuarioRequest:
      type: object
      required: [ identificacion, nombre, email, tipoUsuario ]
      properties:
        identificacion:
          type: string
          example: "1094123456"
        nombre:
          type: string
          minLength: 3
          maxLength: 100
        email:
          type: string
          format: email
          example: "juan.perez@uniquindio.edu.co"
        tipoUsuario:
          $ref: '#/components/schemas/TipoUsuario'

    DesactivarUsuarioRequest:
      type: object
      required: [ tieneSolicitudesActivas ]
      properties:
        tieneSolicitudesActivas:
          type: boolean
          description: Flag para validar si el usuario puede ser desactivado.

    UsuarioResponse:
      type: object
      properties:
        identificacion:
          type: string
        nombre:
          type: string
        email:
          type: string
        tipoUsuario:
          $ref: '#/components/schemas/TipoUsuario'
        activo:
          type: boolean


    TipoUsuario:
      type: string
      enum: [ ESTUDIANTE, DOCENTE, ADMINISTRATIVO, BIENESTAR ]